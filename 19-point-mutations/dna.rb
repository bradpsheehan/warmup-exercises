class DNA

  attr_reader :strand

  def initialize(strand)
    @strand = strand.split("")
  end

  def hamming_distance(mutation)
    # hamming distance will = the total number of
    # number of letter minus the number of letter matches between the two arrays
    mut_letters = mutation.split("")
    mut_letters.each do |letter|

    end
    if mutation == || mutation == ""
      0
    else
      
    end
  end

  # check out clojure


end
